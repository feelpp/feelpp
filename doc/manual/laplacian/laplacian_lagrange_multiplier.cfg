[gmsh]
filename=feel.geo

[functions]
z=sin(pi*x)*cos(pi*y):x:y
p=pi*cos(pi*x)*cos(pi*y):x:y
q=-pi*sin(pi*x)*sin(pi*y):x:y
f=-2*pi^2*sin(pi*x)*cos(pi*y):x:y

[exporter]
format=ensightgold
geometry=0

[exporter.ensightgold]
use-sos=true
